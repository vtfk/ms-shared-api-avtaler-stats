{
  "name": "ms-shared-api-avtaler-stats",
  "description": "Stats service for avtaler",
  "version": "1.2.1",
  "license": "MIT",
  "private": true,
  "author": {
    "name": "Geir Gåsodden",
    "email": "geir.gasodden@pythonia.no",
    "url": "https://github.com/zrrrzzt"
  },
  "engines": {
    "node": ">=10.16.2"
  },
  "scripts": {
    "test": "standard && npm audit && ava",
    "test-offline": "standard && ava",
    "coverage": "nyc ava",
    "coveralls": "nyc ava && nyc report --reporter=lcov && cat coverage/lcov.info | coveralls",
    "standard-fix": "standard --fix",
    "dev": "now dev",
    "deploy": "npm test && now --target production",
    "refresh": "rm -rf node_modules && rm package-lock.json && npm install"
  },
  "keywords": [
    "microservice",
    "stats",
    "Telemark fylkeskommune"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vtfk/ms-shared-api-avtaler-stats.git"
  },
  "bugs": {
    "url": "https://github.com/vtfk/ms-shared-api-avtaler-stats/issues"
  },
  "homepage": "https://github.com/vtfk/ms-shared-api-avtaler-stats#readme",
  "devDependencies": {
    "ava": "2.3.0",
    "coveralls": "3.0.6",
    "nyc": "14.1.1",
    "standard": "14.0.2"
  },
  "dependencies": {
    "mongodb": "3.3.0"
  }
}
